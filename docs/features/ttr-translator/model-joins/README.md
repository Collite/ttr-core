# MJ — Model joins (pointer)

This effort is authored only in the `project/` tracking repo (post-2026-07-15 convention):

`~/Dev/collite-gh/project/tatrman/features/ttr-translator/model-joins/` — `STATUS.md`, `design.md`,
`architecture.md`, `contracts.md`, `plan/plan.md`, `plan/tasks/`.

Code lands in `packages/kotlin/ttr-translator` (`joiner/JoinPolicy`, `codec/sql/ModelJoinRewriter`,
`joiner/JoinerLogical`, `orchestrator/Translator`).
