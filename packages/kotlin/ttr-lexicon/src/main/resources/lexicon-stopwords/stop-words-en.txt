# English function words — the RG-LEX-031 guard (LP contracts §3.1).
#
# See `stop-words-cs.txt` for what this list is for and why it is a deliberate twin
# of `ttr-metadata`'s `/search/stop-words-en.txt` (`StopWordParitySpec` holds them
# equal). One token per line, UTF-8; `#` comments; compared after fold + lowercase.
a
an
the
and
or
is
are
was
were
be
been
being
this
that
these
those
for
on
in
to
from
with
by
at
of
which
what
how
where
when
me
my
it
# review-103 N5 — the bare negator. Never a predicate form on its own (`not` next to a
# literal negates the trigger beside it, it is not one), and `not` + a function word
# (`not with`) is a phrase of function words only, which RG-LEX-031 refuses.
not
